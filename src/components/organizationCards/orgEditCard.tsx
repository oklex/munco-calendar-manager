import React from 'react'

export default class OrgEditCard extends React.Component<{},{}> {
    /* 
    redux:
    - website key ?

    props: (none)

    state:
    - updated organization information : IOrganizationRequest

    functions:
    - onChange ; update state attribute that matches input name
    - save data ; patch data
    - show error ; if save fails show error
    - render ; show the card
    */
    
    render() {
        return (
            <div>organization card prototype</div>
        )
    }
}