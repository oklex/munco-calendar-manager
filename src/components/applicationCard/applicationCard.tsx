import React from 'react'

export default class ApplicationCard extends React.Component<{},{}> {
    /* 
    redux:
    - website key ?

    props:
    - appication ID
    - initial application information : IApplication

    state:
    - updated application information : IApplicationRequest

    functions:
    - onChange ; update state attribute that matches input name
    - save data ; patch by app id
    - show error ; if save fails show error
    - render ; show the card
    */
    
    render() {
        return (
            <div>application card prototype</div>
        )
    }
}